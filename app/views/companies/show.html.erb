<div><strong><%= @company.name %></strong></div>

<div class="btn">
  <%= link_to "back", companies_path, class: "btn btn-primary btn-lg" %>
</div>

<%= link_to company_path(@company), class: "btn btn-primary btn-lg", method: :delete, data: { confirm: "Are you sure you want to delete?" } do %>
<i class="fa fa-trash" aria-hidden="true"></i>
<% end %>
